# Find minimum and maximum from tuple

numbers = tuple(map(int, input("Enter numbers separated by space: ").split()))

minimum = min(numbers)
maximum = max(numbers)

print("Minimum:", minimum)
print("Maximum:", maximum)
